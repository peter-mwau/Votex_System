import React from 'react';

const AlertDescription = ({ children }) => {
    return <div style={{ marginTop: '8px', fontSize: '14px', color: '#555' }}>{children}</div>;
};

export default AlertDescription;